# AuthentiLens Space: CPU-only. Versions match requirements-lock.txt, the
# environment the April results were produced in. torch/torchvision resolve to
# CPU wheels via the extra index URL in the Dockerfile.
torch==2.11.0
torchvision==0.26.0
segmentation_models_pytorch==0.5.0
timm==1.0.26

numpy==2.4.3
pillow==12.2.0
scipy==1.17.1
matplotlib==3.10.8
opencv-python-headless==4.13.0.92

streamlit==1.56.0
psutil==7.2.2
huggingface_hub==1.9.2
